# JVM Memory Allocation
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Disable KSP Incremental Processing to prevent Room round 2 duplicate file exceptions
ksp.incremental=false
android.useAndroidX=true
